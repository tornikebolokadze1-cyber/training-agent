# Session Handoff — 2026-04-24

## Accomplished

This session shifted from "which memory system to pick" (Mem0 vs Graphiti vs Cognee) to the actual critical issue: **the agent was losing its most valuable retrospective data (WhatsApp conversations) on every restart**, and scoring data had 22% of rows with NULL overall_score.

### Data rescue — executed and verified
- **3000 WhatsApp messages rescued** from Green API (G1: 33.14 days / 23 senders, G2: 25.49 days / 24 senders, Tornike DM: 2.8 days) → `data/greenapi_backfill/export_20260424_095902.json` (3.1 MB).
- **5 NULL scores recovered** (G1 L2/L8, G2 L6/L9/L11) via composite fallback — written to `data/scores.db`.
- **Obsidian WhatsApp dumps refreshed** via manual `obsidian_sync --whatsapp` (200 messages synced, was 23-days stale).

### Infrastructure — built and tested
- `data/messages.db` — new SQLite archive, 3000 rows indexed.
- `tools/services/message_archive.py` — standalone ingestion module, 121 statements, **90% test coverage**.
- `tools/tests/test_message_archive.py` — **17/17 tests passing** (hash, normalize, persistence, queries).
- `scripts/migrate_001_messages.sql` — schema; SQLite-now, Postgres-ready.
- `scripts/backfill_messages.py` — idempotent loader (re-run = 0 inserts, 3000 skips).
- `scripts/export_green_api_history.py` — rescue export with summary stats.
- `scripts/probe_green_api_history.py` — read-only diagnostics.
- `scripts/recover_null_scores.py` — dry-run-by-default scores recovery.

### Documentation
- `.claude/plans/postgres_message_archive.md` — full Postgres schema + PII/encryption + hosting decision matrix + 8-file change inventory.
- `WHATSAPP_DATA_GAP_AUDIT.md` — 7-section diagnostic audit.

## Current state

- Git branch: (unchanged — local changes uncommitted)
- App status: production ** untouched**. Zero live code modified in this session. messages.db lives alongside (not inside) the hot path.
- Retrospective querying **works now** locally:
  - "Confusion signals (ვერ/რატომ/არ მესმის) across course": G1=66, G2=64
  - Per-sender joins functional
  - Per-group time windows functional

## Blocked by operator actions

1. **Anthropic credits exhausted** (2026-04-24) — no deep analysis, no agent replies. Top-up required before any automation can resume.
2. **Railway deploy frozen** since 2026-04-16 (invalid RAILWAY_TOKEN). Without this, the Postgres archive cannot migrate to production — only the local SQLite copy works.
3. **Unprocessed lectures** that were missed during the freeze: G1 L11 (partial), G2 L10-L12. These rely on both credits and deploy to be reprocessed.

## Next steps (recommended order)

1. **Top-up Anthropic credits** → unblocks everything downstream.
2. **Regenerate Railway token** → unblocks deploy.
3. **Reprocess missed lectures** (G1 L11, G2 L10-L12) once (1) and (2) land.
4. **Decide hosting for production archive**: Supabase free tier recommended (see plan doc §5).
5. **Wire archive write into webhook** — 8-file scope, requires explicit approval per global rules; staged rollout (INSERT before Mem0, parallel for 1 week, then cut over).
6. **Add `obsidian_sync --whatsapp` to `scheduler.py` cron** (04:00 Tbilisi daily) — small fix, ~20 min.
7. **Contextual Retrieval + Cohere Rerank v4 Pro** for the Pinecone RAG layer (separate plan) — ~$15 one-time indexing + $30-50/mo runtime.

## Important context

- **Today's rescue window was time-sensitive.** Green API rotates older messages; had we waited another week, messages from 2026-03-21 to 2026-03-28 for G1 would likely be gone. They are now permanently in local SQLite.
- **Pre-March-21 / Pre-March-29 window is lost** in raw form. Only Mem0 semantic fragments remain for that period. Pre-course (early Feb) = fully lost, no further recovery possible.
- **The 5 NULL scores** had all 5 dimension values populated — only the LLM-written "საერთო შეფასება" row was missing. Composite (arithmetic mean of 5 dims) was used as the authoritative overall. If re-processing with fresh LLM produces a different overall, re-run with that value will overwrite.
- **Pepper not set** → dev placeholder used in hashes. Before production, set `SENDER_HASH_PEPPER` env and re-hash (decision: rotate entire sender_hash column via `UPDATE`, or treat local DB as ephemeral and rebuild).

## Open questions

- **Supabase vs DigitalOcean** for production Postgres? Plan doc recommends Supabase.
- **Mem0 migration strategy** once archive goes live: run them in parallel (archive = raw, Mem0 = semantic), or sunset Mem0?
- **Retention policy:** hot indefinitely, or archive > 12 months to Drive JSON dump?
- **Consent / GDPR framing** for retrospective analysis of student messages — was this disclosed at enrollment?

## Files touched (session 2026-04-24)

### New
- `scripts/probe_green_api_history.py`
- `scripts/export_green_api_history.py`
- `scripts/recover_null_scores.py`
- `scripts/backfill_messages.py`
- `scripts/migrate_001_messages.sql`
- `tools/services/message_archive.py`
- `tools/tests/test_message_archive.py`
- `.claude/plans/postgres_message_archive.md`
- `WHATSAPP_DATA_GAP_AUDIT.md`
- `.claude/handoff-2026-04-24.md`  ← this file
- `data/messages.db` (new SQLite database, 3000 rows)
- `data/green_api_probe_report.json`
- `data/greenapi_backfill/export_20260424_095902.json` (3.1 MB)

### Modified
- `data/scores.db` — 5 overall_score values filled (G1 L2=6.40, G1 L8=7.20, G2 L6=6.20, G2 L9=6.60, G2 L11=6.00)
- `obsidian-vault/WhatsApp დისკუსიები/ჯგუფი 1 -- ჩატი.md` (refreshed)
- `obsidian-vault/WhatsApp დისკუსიები/ჯგუფი 2 -- ჩატი.md` (refreshed)

### Production code: **ZERO changes**
No `whatsapp_assistant.py`, `server.py`, `scheduler.py`, or `orchestrator.py` edits. The archive module is isolated and not yet imported anywhere in the hot path.

## Approaches tried (for future debugging)

- Initially considered Graphiti/Cognee migration. Rejected as premature once it became clear the root issue was zero-persistence of WhatsApp messages. **Storage integrity precedes memory sophistication.**
- Considered running `/ralph` autonomous loop. Declined because (a) Claude credits exhausted, (b) task shape not PRD-appropriate, (c) 8-file production scope requires per-rule user confirmation.

## Key discoveries

- `whatsapp_assistant.py:141` — `_chat_history: dict[-15:]` is in-memory only. No DB persistence anywhere in the existing codebase.
- Green API `getChatHistory` at count=1000 returns 33 days of G1 / 25 days of G2. Plan tier sufficient for month-long retroactive recovery.
- `scheduler.py` has NO `obsidian_sync` cron — all Obsidian sync is manual-trigger.
- `extract_scores()` regex in `analytics.py` does not match certain "საერთო შეფასება" formatting; fallback to `composite` is sound.
- All 5 NULL-overall rows have **full dimensional scores** — not truly missing data, just an extraction glitch.
