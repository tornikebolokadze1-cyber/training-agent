# Credentials & secrets — NEVER commit these
.env
.env.*
credentials.json
token.json
token.json.backup
token_gmail.json
attendees.json
*.pem
*.key
*.backup

# Temporary files
.tmp/

# Application logs (rotated automatically)
logs/

# Large media files (safety net outside .tmp/)
*.mp4
*.m4a
*.m4v
*.webm
*.wav

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Firebase
firebase-debug.log

# Playwright MCP logs
.playwright-mcp/

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo

# Test coverage artifacts
.coverage
coverage.json
htmlcov/

# Ruff cache
.ruff_cache/

# Process management
.training-agent.pid

# OS
.DS_Store
Thumbs.db
token.json.backup-*

# Obsidian local backup (tonight)
obsidian-vault.backup.*/
# Data dumps & screenshots (large, reproducible)
data/dashboard_screenshots/
data/greenapi_backfill/
data/green_api_probe_report.json

# === PII & student-data protection (added 2026-04-24) ===
# Message archive contains real student phone numbers and conversations
data/messages.db
data/messages.db-shm
data/messages.db-wal
data/greenapi_backfill/
data/green_api_probe_report.json

# Scores DB is low-risk but contains student-derived aggregate data
data/scores.db
data/scores.db-shm
data/scores.db-wal

# Un-redacted mid-course diagnostic (contains real student names + phones)
obsidian-vault/ანალიზი/MID_COURSE_WHATSAPP_PATTERNS.md

# Dashboard screenshots may contain logged-in PII
data/dashboard_screenshots/
